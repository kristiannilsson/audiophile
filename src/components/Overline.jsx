import styled from "styled-components";

const Paragraph = styled.p`
  font-size: 14px;
  letter-spacing: 10px;
  color: var(--sienna);
`;

export default function Overline() {
  return <Paragraph>NEW PRODUCT</Paragraph>;
}
